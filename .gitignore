# Python
__pycache__/
*.py[cod]
*.pyo

# Testing & coverage
.coverage
.pytest_cache/

# Virtual environments
.venv/

# Build
build/
dist/
*.egg-info/

# Keys / secrets
*.key
config.yaml

# Bot state
meshbot.db
meshbot.db-shm
meshbot.db-wal
state_backup_*/

# Legacy JSON state and old DB name (pre-SQLite migration). Kept for
# rollback safety on existing deployments and renamed to *.imported
# after a successful one-shot import on first start.
last_seen.json*
routes_seen.json*
route_stats.json*
adverts_seen.json*
dm_histories.json*
messages.db*

# Local reference repos
ref/

# uv
uv.lock
